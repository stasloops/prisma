import { TasksList } from "widgets/tasks_list";

const Tasks = () => {
  return (
    <main>
      {/* <h1 className="text-title font-bold text-center">Квесты (3)</h1> */}
      <TasksList />
    </main>
  );
};

export default Tasks;
